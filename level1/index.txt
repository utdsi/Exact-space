
1. find / -name "*.log" 
   (All files with .log extension)

2. find / -name "*.log" -exec du -h {} + 
   (All files with sizes)

3. find / -name "*.log" -exec du -h {} + | sort -rh | head -n 1
   (Getting the largest log file)

4. cat temp/WinSAT/.log
   (opening the required log file)

5. head -n 100 temp/WinSAT/.log
   (truncating and displaying first 100 lines)

